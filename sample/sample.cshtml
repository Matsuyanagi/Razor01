@using System.Text.RegularExpressions;
Hello @(Model.Name), welcome to RazorEngine! from file.
@*
    razor text comment
	[C# Razor構文 基礎文法 総まとめ - ＠IT](http://www.atmarkit.co.jp/fdotnet/rapidmaster/rapidmaster_03/rapidmaster_03.html)
	[ASP.NET Core の Razor 構文リファレンス | Microsoft Docs](https://docs.microsoft.com/ja-jp/aspnet/core/mvc/views/razor?view=aspnetcore-2.1)
	[チュートリアル: Visual Studio での Razor 構文を使用した Web サイトの作成](https://msdn.microsoft.com/ja-jp/library/gg606533(v=vs.100).aspx)

*@
@{
	for( int i = 0; i < 5; i++ ){
		// インデントあり。
		<text>no : @i</text>
		// インデントなし。@() 内の文字列を出力する
		@("no : " + i.ToString() + "\n")
	}
}
---- ----
@for( int j = 0; j < 10; j++ ){
	if ( j == 0 ){
		// zero 出力。インデントされている
		@:zero
		// zero 出力。インデントされている
		<text>zero</text>
		// zero 出力。インデントされてないし、改行もしていない。
		@("zero")
	}else if ( ( j & 1 ) == 0 ){
<text>even : j = @j</text>
	}else{
	<text>odd  : j = @j</text>
	}
}
---- ----
0 < 100
---- ----
<element></element>
---- ----
@{
	var a = 100;
	// 'a'という文字が出力される。インデントされてる
	@:a
	// 100 が出力される。インデントされてない。
	@(a)
	@:

	var e = "<element></element>";
	// デフォルトだと &lt;element&gt;&lt;/element&gt;
	// config.EncodedStringFactory = new RawStringFactory(); だと <element></element>
	@(e)
	@:
	// デフォルトだと &lt;element&gt;&lt;/element&gt;
	// config.EncodedStringFactory = new RawStringFactory(); だと <element></element>
	@("<element></element>")
	@:
	// インデントされる <element></element>
	@:<element></element>
	// インデントされる "<element></element>"
	@:"<element></element>"


}